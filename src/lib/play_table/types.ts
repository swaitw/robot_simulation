export type PlayTableSize = {
  width: number;
  height: number;
};

export type PointOfPlayTable = {
  x: number;
  y: number;
};

export type OverEdgeResult = {
  isOver: boolean;
  value: number;
};
